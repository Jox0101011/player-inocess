player_install() {
   sudo pacman -S npm
   sudo npm -g live-server
  }

player_web() {
  live-server web/

  echo -e "[+] ON"
  }

player() {
  case "$1" in
  web) player_web ;;
  install) player_install ;;
  *) echo "\e[31m[!] Usage: player [web|install]\e[0m" ;;

  esac
  }
